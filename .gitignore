dist/
node_modules/
.env
.env.local

# Screenshots from local browser/page-screenshot testing - keep out of commits.
screenshot.png
ss-*.png

# gw worktrees (git tracks them via .git/worktrees metadata; the dir itself is scratch)
.worktrees/
